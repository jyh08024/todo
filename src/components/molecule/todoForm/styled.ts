import styled from "styled-components";

export const TodoFormStyle = styled.div`
  width: 100%;

  .enr_form {
    padding: 0 3rem;
  }

  input {
    width: 85%;
    font-size: 1.4rem;
    font-family: "noto";
  }

  input::placeholder {
    color: rgba(255, 255, 255, .5);
  }
`;